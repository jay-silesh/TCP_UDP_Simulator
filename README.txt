
I have created a make file and the TA has to type only "Makefile" (NOTE: the 'M' is capital in the "Makefile" command)
   on the terminal and run each of the file  by typing "warehouse" "store1" "store2" "store3" "store4"

